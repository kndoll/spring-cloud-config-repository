kndoll:
  profile: I'm stg - update
  comment: stg config - update
server:
  port: 8089
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: kndoll
    password: doll
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
